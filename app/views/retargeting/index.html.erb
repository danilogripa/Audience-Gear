<% if @affiliates.count > 0 %>
    <% @affiliates.each do |affiliate| %>
        <%= "// #{affiliate.name}\n"%>
        <%= raw affiliate.partner %>
        <%= "\n// END #{affiliate.name}\n"%>
    <% end %>
<% else %>
  // Afiliados desativados ou inexistentes
<% end %>
